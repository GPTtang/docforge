# 代码更新记录

## 2026-03-15

### 更新概览
- 新增 `.doc`、`.xls`、`.xlsx`、`.ppt` 的转换支持。
- 保持 `.pdf`、`.docx`、`.pptx` 的原有能力并做稳定性优化。
- 全量测试文档回归通过：`26/26`。

### 主要变更
1. Python 服务增强
- 新增 `python-service/converters/office_converter.py`：通过 LibreOffice（`soffice --headless`）将旧 Office 格式转为新格式。
- 新增 `python-service/converters/spreadsheet_converter.py`：使用 `openpyxl` 解析表格并导出 Markdown/JSON。
- 重构 `python-service/main.py`：按扩展名路由到 Docling/Marker/Office/Spreadsheet 转换链路。
- 更新 `python-service/requirements.txt`：新增 `openpyxl`。
- 更新 `python-service/Dockerfile`：安装 LibreOffice 相关组件。

2. Java 网关改进
- 新增 `java-service/src/main/java/com/docforge/service/PythonServiceException.java`。
- 更新 `DocConvertService.java`：透传下游 HTTP 状态码与错误信息。
- 更新 `DocForgeController.java`：按下游状态码返回，优化异常处理与注入方式。

3. 启动与运维优化
- 更新 `docker-compose.yml`：将 Python 健康检查 `start_period` 从 `60s` 调整为 `180s`。
- 新增 `.gitignore`：忽略本地/构建产物（如 `.claude/`、`**/target/`、`test-documents/reports/`）。

### 测试结果
- 测试目录：`test-documents/`
- 覆盖格式：`.pdf/.doc/.docx/.xls/.xlsx/.ppt/.pptx`
- 全量结果：`26/26 PASS`
- 报告：`test-documents/reports/api-smoke-20260314-225329.csv`
